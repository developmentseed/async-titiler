services:
  api:
    build:
      context: .
    ports:
      - "8081:8081"
    command: ["uvicorn", "async_titiler.main:app", "--host", "0.0.0.0", "--port", "8081", "--workers", "1"]

  benchmark:
    extends:
      service: api
    volumes:
      - ./.github/data:/data
